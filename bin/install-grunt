#!/bin/bash

projectDir="./www"

files=("package.json.sample" "grunt-config.json.sample" "Gruntfile.js.sample")

for file in "${files[@]}"; do
  filesWithOutSample="${file%.sample}"

  # Copy the file to the same directory with the new name
  cp "$projectDir/$file" "$projectDir/$filesWithOutSample"
done